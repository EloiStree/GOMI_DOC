- `at_ready` La commande sera chargée dans GOMI au `_ready()` de Godot, c'est-à-dire lorsque la scène est chargée et importée dans la scène.

```
FILE>>>|.godot_event_to_command_at_ready
cmd:log AT_READY EVENT


```
