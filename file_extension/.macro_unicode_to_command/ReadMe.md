
```
FILE>>>|.unicode_to_command
# Use `u⌘` to call the macro  `u⌘🔴⬇️ 10000> u⌘📷 100> u⌘🔴⬆️`
# macro: check for string macro and other special macro
#umacro: Check for Unicode macro
#cmacro: Check for character macro
#imacro: Chack for integer macro

🔴⬇️ ♦️ cmd:log start recording
🔴⬆️ ♦️ cmd:log stop recording
📷 ♦️ cmd:log take screenshot
```
